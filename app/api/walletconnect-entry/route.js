
export async function POST(req) {
  // placeholder for walletconnect events
  return new Response(JSON.stringify({ ok: true, note: "walletconnect placeholder" }), { status: 200, headers: { "Content-Type": "application/json" } });
}
