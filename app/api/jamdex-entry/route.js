export async function POST(req) {
  // placeholder stub, returns ok: true
  return new Response(JSON.stringify({ ok: true, note: "jamdex placeholder" }), { status: 200, headers: { "Content-Type": "application/json" } });
}
