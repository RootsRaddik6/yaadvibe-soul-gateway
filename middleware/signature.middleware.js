export async function verifySignature({ address, signature, message }) {
  // TODO: implement TON/Tezos signature verification
  // For PoC return true; replace with real verification using libs:
  // - Tezos: @taquito/utils / @airgap/beacon-sdk verify
  // - TON: tonweb / ton-client-js verify
  return true;
}
